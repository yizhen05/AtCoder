#include <bits/stdc++.h>
using namespace std;

int main()
{
	int N;
	cin >> N;
	vector<int> C(N + 1);
	vector<int> P(N + 1);
	for (int i = 1; i <= N; i++) cin >> C[i] >> P[i];
	int Q;
	cin >> Q;
	vector<int> L(Q + 1);
	vector<int> R(Q + 1);
	for (int i = 1; i <= Q; i++) cin >> L[i] >> R[i];

	for (int i = 1; i <= N; i++)
	{
	}
}

int hoge(int a)
{
	return a;
}